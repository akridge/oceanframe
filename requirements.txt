fastapi>=0.111            # API framework for upload, stream, export endpoints
uvicorn[standard]>=0.29   # ASGI server used to run the FastAPI app
python-multipart>=0.0.9   # Handles multipart/form-data file uploads
jinja2>=3.1               # Server-side HTML template rendering
opencv-python>=4.9        # Video decode, frame reads, and image processing
numpy>=1.26               # Numeric arrays and frame math utilities
Pillow>=10.3              # Thumbnail/image encode and resize helpers
piexif>=1.1               # Optional EXIF metadata embedding on JPEG export